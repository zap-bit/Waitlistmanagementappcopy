# Server
PORT=8000

# Supabase (Option A: backend owns DB access)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: used when validating incoming bearer tokens against Supabase JWTs
SUPABASE_JWT_SECRET=your-jwt-secret
