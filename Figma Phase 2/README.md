
  # Waitlist Management App-with login (Copy)

  This is a code bundle for Waitlist Management App-with login (Copy). The original project is available at https://www.figma.com/design/thDWhVZKXb6MotKXq5OuRC/Waitlist-Management-App-with-login--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  